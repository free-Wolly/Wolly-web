export interface AnimateTextProps {
  title: string;
  delay: number;
  inView: boolean;
  textBackgroundElementWidth: string;
  staggerChildren: number;
}

export interface SectionHeaderProps {
  topTitle: string;
  botTitle: string;
  delay: number;
  textBackgroundElementWidth: string;
  staggerChildren: number;
}
